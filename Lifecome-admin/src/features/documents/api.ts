// TanStack Query hooks for the "documents" feature (useQuery/useMutation wrapping
// src/lib/api/client.ts calls to Lifecome-backend's documents module). Not
// implemented yet - this file exists so the feature folder's shape is
// settled before the real data-fetching code is written.
export {};
