# Copy to .env for local development: cp .env.example .env

# --- Core ---
NODE_ENV=development
PORT=3001
# Comma-separated list of allowed origins for the public web app(s)
CORS_ORIGIN=http://localhost:3000
LOG_LEVEL=debug

# --- Data ---
DATABASE_URL=postgres://lifecome:lifecome@localhost:5432/lifecome_dev
REDIS_URL=redis://localhost:6379

# --- Auth (placeholder until the identity provider, e.g. Keycloak, is chosen — see docs/architecture) ---
SESSION_JWT_SECRET=dev-only-change-me

# --- Integrations (all optional in development; each service degrades or stubs without one) ---
# PAYSTACK_SECRET_KEY=
# FLUTTERWAVE_SECRET_KEY=
# TERMII_API_KEY=
# LIVEKIT_API_KEY=
# LIVEKIT_API_SECRET=
# LIVEKIT_URL=
