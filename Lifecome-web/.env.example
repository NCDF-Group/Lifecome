# Canonical public origin (used for metadata, sitemap and robots)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Authenticated patient app origin. Leave unset until it exists; the Sign In link is hidden while unset.
# NEXT_PUBLIC_PATIENT_APP_URL=https://app.example.com
